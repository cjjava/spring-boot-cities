---
platform: linux

image_resource:
  type: docker-image
  source: {repository:java}

inputs:
- name: cities-repo

run:
  path: bash
  args:
  - -c
  - |
    cd cities-repo/cities-service
    ./gradlew build